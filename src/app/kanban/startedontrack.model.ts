
export class Startedontrack {
    constructor(public name: string, public id: string, public tasks: string[]) {}
}

export class Startdoarray {
    constructor(public name: string, public id: string, public tasks: any) {}
}